tup=(1,2,3,4,5)
print(tup)
print(type(tup))
# we can define a tuple using  ( ) and without brackets

max(tup)  #to check max value
min(tup)  # to check min value
len(tup)  # to check the length
print(max(tup),min(tup),len(tup))

#to print the elements of a tuple
print(tup[1])

# elements can be accessed

#elements cannot be updated ,hence it is immutable  # THE ONLY DATA COLLECTION THAT IS IMMUTABLE

# used to store secret keys

#tuple keeps same order

a=1,2,'chikku',8,5
print(a)